[Unit]
Description=NAS Desktop shell (static files + Glances proxy)
After=network.target

[Service]
Type=simple
User=@SERVICE_USER@
WorkingDirectory=@INSTALL_DIR@
EnvironmentFile=-/etc/nas-control-plane.conf
ExecStart=/usr/bin/python3 @INSTALL_DIR@/serve.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
